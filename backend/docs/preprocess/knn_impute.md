# knn_impute

這一份文件說明此預處理步驟的用途與參數。

## 類別: preprocess

## 說明

以 K 近鄰演算法填補缺值。對每個缺值樣本，找出 K 個最近鄰（依非缺值欄位計算距離），以鄰居的數值取平均作為填充值。比 mean/median 填補更精確，適合特徵間有相關性的醫學資料。

## 可用參數

- `n_neighbors`（整數，預設 5）：用於填補的鄰近樣本數，值越大越穩健但計算越慢

## 範例

```json
{ "type": "knn_impute", "n_neighbors": 5 }
```
